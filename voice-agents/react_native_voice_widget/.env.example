# Rename to .env and fill in. The EXPO_PUBLIC_* pair is inlined into the
# JS bundle at build time by Metro. Both are required by the widget.
EXPO_PUBLIC_SMALLEST_API_KEY=sk_your_key_here
EXPO_PUBLIC_AGENT_ID=your_agent_id_here
